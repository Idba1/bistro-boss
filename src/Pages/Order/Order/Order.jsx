import { Helmet } from "react-helmet-async";

const Order = () => {
    return (
        <div>
            <Helmet>
                <title>HOME | ORDER</title>
            </Helmet>
        </div>
    );
};

export default Order;